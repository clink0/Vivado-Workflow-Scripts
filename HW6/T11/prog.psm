FOREVER:
        input   s0, 00;
        load    s1, 00;
        load    s2, 08;
LOOP:
        test    s0, 01;
        jump    z, SKIP;
        add     s1, 01;
SKIP:
        sr0     s0;
        sub     s2, 01;
        jump    nz, LOOP;
        output  s1, 00;
        jump    FOREVER;
