FOREVER:
        input   s0, 00;
        load    s2, 05;
        load    s3, 06;
        load    s1, s0;
        test    s1, 01;
        call    nz, TESTING; 
        output  s2, 00;
        jump    FOREVER
TESTING:
        output  s3, 00;
        return;
