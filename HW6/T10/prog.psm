        load    s2, 08;
        load    s3, 00;
FOREVER:
        compare s2, 00;
        jump    nz, NOTZ;
        jump    z,  ISZ;
NOTZ:
        sub     s2, 01;
        add     s3, 01;
        jump    FOREVER
ISZ:
        output  s3, 00;
        jump    FOREVER
